#!/usr/bin/env bash

set -e

DEBUG=${DEBUG:-''}
[ -n "${DEBUG}" ] && set -x

# set SCRIPT_PATH
SCRIPT_PATH="$(cd $(dirname "$0")/$(dirname "$(readlink "$0")") && pwd)"

SNAPSHOT=${1:-''}

echo "Delete snapshot $(basename ${SNAPSHOT})"
btrfs subvolume delete ${SNAPSHOT} > /dev/null
